    chain-state-db-size-mb = 65536
    reversible-blocks-db-size-mb = 2048
    #contracts-console = false

    http-server-address = 0.0.0.0:8888
    p2p-listen-endpoint = 0.0.0.0:9876
    p2p-server-address = "TO_BE_FILLED_AS_YOUR_OWN_PUBLIC_IP"

    plugin = eosio::history_plugin
    plugin = eosio::history_api_plugin
    plugin = eosio::chain_api_plugin
    plugin = eosio::chain_plugin
    plugin = eosio::net_plugin
    plugin = eosio::net_api_plugin
    plugin = eosio::http_plugin

    agent-name = sh-exp-agent
    p2p-peer-address = m1.mgps.me:9876
    p2p-peer-address = m2.mgps.me:9876
    p2p-peer-address = m3.mgps.me:9876

    http-validate-host = false
    verbose-http-errors = true

    chain-threads = 8
    http-threads = 6

    access-control-allow-origin = *
    access-control-allow-headers = Origin, X-Requested-With, Content-Type, Accept
    # access-control-max-age =
    # access-control-allow-credentials = false
    #    wasm-runtime = wabt

    #produce-time-offset-us = 250000
    last-block-time-offset-us = -300000

    #chain-state-db-guard-size-mb = 128
    #reversible-blocks-db-guard-size-mb = 2

    p2p-max-nodes-per-host = 150

    #allowed-connection = any

    max-clients = 150
    #connection-cleanup-period = 30
    #network-version-match = 0
    sync-fetch-span = 2000
    enable-stale-production = true

    #pause-on-startup = false
    max-transaction-time = 1000
    #max-irreversible-block-age = -1
    #txn-reference-block-lag = 0
    http-max-response-time-ms = 300

    plugin = eosio::http_plugin
    plugin = eosio::chain_plugin
    plugin = eosio::chain_api_plugin
    plugin = eosio::history_plugin
    plugin = eosio::history_api_plugin
    #plugin = eosio::state_history_plugin
    plugin = eosio::net_plugin
    plugin = eosio::net_api_plugin
    filter-on = *